{
  "name": "pr_corsoc-docs",
  "version": "0.0.0",
  "description": "Utility scripts for generating PDFs from docs/*.html",
  "scripts": {
    "pdf": "node scripts/generate-pdfs.js"
  },
  "devDependencies": {
    "puppeteer": "^21.0.0"
  }
}